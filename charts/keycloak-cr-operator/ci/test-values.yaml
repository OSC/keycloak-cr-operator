---
manager:
  image:
    tag: latest
  config:
    keycloakURL: http://keycloak.keycloak.svc.cluster.local
    adminUsername: admin
    adminPassword: secret
    adminRealm: master
    defaultRealm: master
    clientIdPrefix: kubernetes
  env:
    - name: FOO
      value: bar
